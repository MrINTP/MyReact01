const a = {
  age: 20,
  color: "red",
};

console.log(`${a.age} 그리고 ${a.color}`);

const [age, color] = [22, "blue"];
const [hiage, hicolor] = [1, "green"];

console.log(`${age} 그리고 ${color}`);
console.log(`${hiage} 그리고 ${hicolor}`);
